import template from './authorList.html';
import controller from './authorList.controller';
import './authorList.scss';

let authorListComponent = {
  bindings: {},
  template,
  controller
};

export default authorListComponent;
